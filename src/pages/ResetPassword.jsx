import React,{useState}from'react';import{useNavigate}from'react-router-dom';import{supabase}from'../lib/supabase.js';import{GraduationCap}from'lucide-react';export default function ResetPassword(){const[password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[loading,setLoading]=useState(false),[error,setError]=useState(''),navigate=useNavigate();async function save(){if(password.length<6)return setError('A senha deve ter pelo menos 6 caracteres.');if(password!==confirm)return setError('As senhas não coincidem.');setLoading(true);const{error:e}=await supabase.auth.updateUser({password});setLoading(false);if(e)return setError('Não foi possível alterar a senha: '+e.message);alert('Senha alterada com sucesso.');navigate('/')}return <main style={s.page}><section style={s.card}><div style={s.logo}><GraduationCap size={28}/></div><h2>Criar nova senha</h2><p>Digite a nova senha da sua conta StudyMatch.</p>{error&&<div style={s.error}>{error}</div>}<input type="password" placeholder="Nova senha" value={password} onChange={e=>setPassword(e.target.value)}/><input type="password" placeholder="Confirme a nova senha" value={confirm} onChange={e=>setConfirm(e.target.value)}/><button className="btn-primary" disabled={loading} onClick={save}>{loading?'Salvando...':'Alterar senha'}</button></section></main>}const s={page:{minHeight:'100vh',display:'grid',placeItems:'center',padding:20,background:'#F5F8FC'},card:{width:'100%',maxWidth:420,background:'white',padding:28,borderRadius:24,display:'grid',gap:13,boxShadow:'0 20px 60px #082a5518'},logo:{width:55,height:55,borderRadius:17,display:'grid',placeItems:'center',background:'linear-gradient(135deg,#1456A0,#10A77B)',color:'white'},error:{background:'#FFF1F2',color:'#D83A4A',padding:10,borderRadius:11,fontSize:12}};