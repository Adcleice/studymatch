import React,{useEffect,useState}from'react';
export default function NetworkStatus(){const[online,setOnline]=useState(()=>navigator.onLine);useEffect(()=>{const on=()=>setOnline(true),off=()=>setOnline(false);window.addEventListener('online',on);window.addEventListener('offline',off);return()=>{window.removeEventListener('online',on);window.removeEventListener('offline',off)}},[]);if(online)return null;return <div role="status" style={s.bar}>Sem conexão. Algumas ações ficarão disponíveis quando a internet voltar.</div>}
const s={bar:{position:'fixed',top:'max(8px, env(safe-area-inset-top))',left:'50%',transform:'translateX(-50%)',zIndex:7000,width:'min(calc(100% - 24px),520px)',padding:'9px 12px',borderRadius:12,background:'#12213A',color:'#fff',fontSize:11,fontWeight:700,textAlign:'center',boxShadow:'0 8px 24px rgba(0,0,0,.18)'}};